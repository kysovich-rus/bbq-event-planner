<%= link_to (image_tag photo.photo.variant(resize_to_fill: [150, 150]), class: "example-image border border-2 rounded-3 border-dark"),
            url_for(photo.photo),
            data: { lightbox: 'photo-set', title: "
                    #{t('messages.photo.added_by')}
                    #{link_to(photo.user.name, user_path(photo.user))}
                    #{button_to(bi_icon("trash-fill"),
                                event_photo_path(photo.event, photo),
                                method: :delete,
                                title: t('buttons.photo.destroy'),
                                form: { data: { turbo_confirm: t('photos.photo.destroy_confirm') } },
                                class: "btn btn-danger p-1",
                                form_class: 'd-inline') if current_user_can_edit?(photo)}
            "
            } %>

